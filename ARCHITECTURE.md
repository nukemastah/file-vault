# 🏗️ Architecture Diagram

This document provides detailed architecture diagrams and flow explanations for the Secure P2P File Vault.

---

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        SECURE P2P FILE VAULT                    │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐          ┌──────────────────┐          ┌──────────────────┐
│   Sender Browser │          │  Signaling Server│          │ Receiver Browser │
│                  │          │   (Go/WebSocket) │          │                  │
│  ┌────────────┐  │          │                  │          │  ┌────────────┐  │
│  │ Web Crypto │  │          │  ┌────────────┐  │          │  │ Web Crypto │  │
│  │    API     │  │          │  │  Session   │  │          │  │    API     │  │
│  └────────────┘  │          │  │  Manager   │  │          │  └────────────┘  │
│        ↕         │          │  └────────────┘  │          │        ↕         │
│  ┌────────────┐  │          │        ↕         │          │  ┌────────────┐  │
│  │  WebRTC    │  │◄─────────┼────────┴─────────┼─────────►│  │  WebRTC    │  │
│  │   Peer     │  │ Signaling│                  │Signaling │  │   Peer     │  │
│  └────────────┘  │          └──────────────────┘          │  └────────────┘  │
│        ║         │                                         │        ║         │
│  ┌────────────┐  │                                         │  ┌────────────┐  │
│  │DataChannel │  │                                         │  │DataChannel │  │
│  └────────────┘  │                                         │  └────────────┘  │
└──────────────────┘                                         └──────────────────┘
         ║                                                            ║
         ║                    P2P Encrypted Transfer                 ║
         ╚════════════════════════════════════════════════════════════╝
                            (DTLS + AES-GCM)
```

---

## Component Interaction Flow

### 1. Session Initialization

```
Sender                    Server                    Receiver
  │                         │                          │
  ├─── WebSocket Connect ──→│                          │
  │                         │                          │
  ├─── Join (role:sender) ─→│                          │
  │                         │                          │
  │                         ├─ Create Session          │
  │                         ├─ Generate SessionID      │
  │                         │                          │
  │←─── Ready (sessionId) ──┤                          │
  │                         │                          │
  │                         │                          │
  │                         │←─── WebSocket Connect ───┤
  │                         │                          │
  │                         │←─── Join (role:receiver)─┤
  │                         │      + sessionId         │
  │                         │                          │
  │                         ├─ Match to Session        │
  │                         │                          │
  │                         ├──── Ready ──────────────→│
  │                         │                          │
```

### 2. WebRTC Connection Establishment

```
Sender                    Server                    Receiver
  │                         │                          │
  ├─ Create PeerConnection  │                          │
  ├─ Create DataChannel     │                          │
  ├─ Generate SDP Offer     │                          │
  │                         │                          │
  ├────── Offer ───────────→│                          │
  │                         │                          │
  │                         ├────── Offer ────────────→│
  │                         │                          │
  │                         │                          ├─ Set Remote Desc
  │                         │                          ├─ Create Answer
  │                         │                          │
  │                         │←────── Answer ───────────┤
  │                         │                          │
  │←────── Answer ──────────┤                          │
  │                         │                          │
  ├─ Set Remote Desc        │                          │
  │                         │                          │
  ├─────── ICE Candidates ─→│─────── ICE Candidates ──→│
  │←─────── ICE Candidates ─┤←─────── ICE Candidates ──┤
  │                         │                          │
  ├══════ DataChannel OPEN ══════════════════════════  ┤
  │                         │                          │
```

### 3. File Transfer Flow

```
Sender                                                Receiver
  │                                                      │
  ├─ Generate AES-GCM Key                               │
  ├─ Generate Random IV                                 │
  ├─ Calculate SHA-256 Hash                             │
  │                                                      │
  ├──────────── Metadata (JSON) ────────────────────────→│
  │  {                                                   │
  │    encryptionKey: [...],                            ├─ Import Key
  │    iv: [...],                                       ├─ Prepare Receiver
  │    hash: "...",                                     │
  │    name: "file.pdf",                                │
  │    size: 1048576                                    │
  │  }                                                   │
  │                                                      │
  ├─ Encrypt Chunk 0 ────────────────────────────────→  ├─ Store Chunk 0
  ├─ Encrypt Chunk 1 ────────────────────────────────→  ├─ Store Chunk 1
  ├─ Encrypt Chunk 2 ────────────────────────────────→  ├─ Store Chunk 2
  │      ...                                             │      ...
  ├─ Encrypt Chunk N ────────────────────────────────→  ├─ Store Chunk N
  │                                                      │
  ├──────────── Complete (JSON) ────────────────────────→│
  │                                                      │
  │                                                      ├─ Decrypt All Chunks
  │                                                      ├─ Reassemble File
  │                                                      ├─ Calculate Hash
  │                                                      ├─ Verify Hash
  │                                                      │
  │                                                      ├─ Create Blob
  │                                                      ├─ Ready for Download
  │                                                      │
```

---

## Data Flow Diagrams

### Encryption Flow

```
┌─────────────────┐
│  Original File  │
│   (200 KB)      │
└────────┬────────┘
         │
         ↓
   ┌─────────┐
   │ SHA-256 │───────────┐
   └────┬────┘           │
        │                │ Store hash
        │                ↓ for verification
        │          ┌──────────┐
        │          │ Metadata │
        │          └──────────┘
        │
        ↓
┌───────────────┐
│ Split Chunks  │
└───────┬───────┘
        │
        ↓
  ┌─────────────┬─────────────┬─────────────┐
  │  Chunk 0    │  Chunk 1    │  Chunk 2    │
  │  (64 KB)    │  (64 KB)    │  (72 KB)    │
  └─────┬───────┴─────┬───────┴─────┬───────┘
        │             │             │
        ↓             ↓             ↓
  ┌──────────┐  ┌──────────┐  ┌──────────┐
  │AES-GCM   │  │AES-GCM   │  │AES-GCM   │
  │Encrypt   │  │Encrypt   │  │Encrypt   │
  │IV=base^0 │  │IV=base^1 │  │IV=base^2 │
  └────┬─────┘  └────┬─────┘  └────┬─────┘
       │            │            │
       ↓            ↓            ↓
  ┌──────────┐ ┌──────────┐ ┌──────────┐
  │Encrypted │ │Encrypted │ │Encrypted │
  │Chunk 0   │ │Chunk 1   │ │Chunk 2   │
  │+16B tag  │ │+16B tag  │ │+16B tag  │
  └────┬─────┘ └────┬─────┘ └────┬─────┘
       │            │            │
       └────────────┴────────────┘
                    │
                    ↓
          ┌─────────────────┐
          │  DataChannel    │
          │  (DTLS-secured) │
          └─────────────────┘
```

### Decryption Flow

```
          ┌─────────────────┐
          │  DataChannel    │
          │  (DTLS-secured) │
          └────────┬────────┘
                   │
         ┌─────────┴─────────┐
         │                   │
         ↓                   ↓
   ┌──────────┐        ┌──────────┐
   │ Metadata │        │ Encrypted│
   │          │        │  Chunks  │
   │ Extract: │        └────┬─────┘
   │ - Key    │             │
   │ - IV     │             ↓
   │ - Hash   │   ┌──────────────────┐
   └────┬─────┘   │ Store in memory  │
        │         │ (receivedChunks) │
        │         └────────┬─────────┘
        │                  │
        │                  ↓
        │         ┌─────────────────┐
        │         │ Decrypt Chunk 0 │◄─┐
        │         │   with IV^0     │  │
        │         └────────┬────────┘  │ Loop
        │                  ↓           │ all
        │         ┌─────────────────┐  │ chunks
        │         │ Decrypt Chunk 1 │  │
        │         │   with IV^1     │  │
        │         └────────┬────────┘  │
        │                  │           │
        │                 ...         ─┘
        │                  │
        │                  ↓
        │         ┌──────────────┐
        │         │ Reassemble   │
        │         │ File Blob    │
        │         └──────┬───────┘
        │                │
        └────────────────┤
                         ↓
                  ┌────────────┐
                  │ Calculate  │
                  │  SHA-256   │
                  └──────┬─────┘
                         │
                         ↓
                  ┌────────────┐
                  │  Compare   │
                  │   Hashes   │
                  └──────┬─────┘
                         │
                ┌────────┴────────┐
                ↓                 ↓
           ┌────────┐        ┌────────┐
           │ MATCH  │        │MISMATCH│
           │   ✅   │        │   ❌   │
           └────┬───┘        └────┬───┘
                │                 │
                ↓                 ↓
          ┌──────────┐      ┌──────────┐
          │ Download │      │  Error   │
          │  Ready   │      │  Reject  │
          └──────────┘      └──────────┘
```

---

## Security Layers

```
┌────────────────────────────────────────────────────┐
│              APPLICATION LAYER                     │
│  ┌──────────────────────────────────────────────┐ │
│  │  End-to-End Encryption (AES-GCM-256)        │ │
│  │  - File chunks encrypted before transmission │ │
│  │  - Keys never leave browser                  │ │
│  │  - SHA-256 integrity verification            │ │
│  └──────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────┘
                        ↕
┌────────────────────────────────────────────────────┐
│              WEBRTC LAYER                          │
│  ┌──────────────────────────────────────────────┐ │
│  │  DTLS (Datagram Transport Layer Security)   │ │
│  │  - Authenticates peers                       │ │
│  │  - Encrypts DataChannel transport            │ │
│  │  - Perfect Forward Secrecy                   │ │
│  └──────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────┘
                        ↕
┌────────────────────────────────────────────────────┐
│              TRANSPORT LAYER                       │
│  ┌──────────────────────────────────────────────┐ │
│  │  SCTP over UDP (or TCP)                      │ │
│  │  - Reliable, ordered delivery                │ │
│  │  - Congestion control                        │ │
│  └──────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────┘

Note: Two layers of encryption!
1. Application: AES-GCM (protects content from server)
2. Transport: DTLS (protects channel from network)
```

---

## Session State Machine

```
┌─────────────┐
│   INITIAL   │
└──────┬──────┘
       │
       │ User selects role
       ↓
┌──────────────┐
│ ROLE_SELECTED│
└──────┬───────┘
       │
       ├──→ Sender: Create Session
       │    └──→ Generate SessionID
       │         Generate Encryption Key
       │
       └──→ Receiver: Enter SessionID
            
       │
       ↓
┌──────────────┐
│  CONNECTING  │
└──────┬───────┘
       │
       │ WebSocket Connect
       │ Send Join Message
       │
       ↓
┌──────────────┐
│   SIGNALING  │
└──────┬───────┘
       │
       │ Exchange SDP Offers/Answers
       │ Exchange ICE Candidates
       │
       ↓
┌──────────────┐
│  CONNECTED   │
└──────┬───────┘
       │
       ├──→ Sender: Wait for file selection
       │              │
       │              ↓
       │    ┌──────────────────┐
       │    │  TRANSFERRING    │
       │    └──────────┬───────┘
       │               │
       │               │ Send Metadata
       │               │ Encrypt & Send Chunks
       │               │
       │               ↓
       │    ┌──────────────────┐
       │    │    COMPLETE      │
       │    └──────────────────┘
       │
       └──→ Receiver: Wait for file
                      │
                      ↓
           ┌──────────────────┐
           │    RECEIVING     │
           └──────────┬───────┘
                      │
                      │ Receive Metadata
                      │ Receive Chunks
                      │
                      ↓
           ┌──────────────────┐
           │   DECRYPTING     │
           └──────────┬───────┘
                      │
                      │ Decrypt All Chunks
                      │ Verify Hash
                      │
                      ↓
           ┌──────────────────┐
           │   VERIFIED       │
           └──────────┬───────┘
                      │
                      │ User Downloads
                      │
                      ↓
           ┌──────────────────┐
           │    COMPLETE      │
           └──────────────────┘

Error/Disconnect at any stage:
       │
       ↓
┌──────────────┐
│    ERROR     │
└──────────────┘
```

---

## Backend Architecture

```
┌────────────────────────────────────────────────┐
│               Go Server (main.go)              │
├────────────────────────────────────────────────┤
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │        HTTP Server (port 8080)           │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  GET  /               → Serve Frontend  │ │
│  │  POST /api/session    → Create Session  │ │
│  │  WS   /ws             → WebSocket        │ │
│  │                                          │ │
│  └──────────────┬───────────────────────────┘ │
│                 │                              │
│                 ↓                              │
│  ┌──────────────────────────────────────────┐ │
│  │       WebSocket Handler                  │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  • handleWebSocket(conn)                │ │
│  │  • peerWriter(peer)                     │ │
│  │  • handleSignalMessage(msg)             │ │
│  │                                          │ │
│  └──────────────┬───────────────────────────┘ │
│                 │                              │
│                 ↓                              │
│  ┌──────────────────────────────────────────┐ │
│  │        Session Manager                   │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  sessions: map[sessionID]*Session       │ │
│  │                                          │ │
│  │  • CreateSession()                      │ │
│  │  • AddPeer(sessionID, peer)             │ │
│  │  • RemovePeer(sessionID, peerID)        │ │
│  │  • BroadcastToSession(sessionID, msg)   │ │
│  │  • cleanupExpiredSessions()             │ │
│  │                                          │ │
│  └──────────────┬───────────────────────────┘ │
│                 │                              │
│                 ↓                              │
│  ┌──────────────────────────────────────────┐ │
│  │          Session Model                   │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  • ID: string                           │ │
│  │  • Sender: *Peer                        │ │
│  │  • Receiver: *Peer                      │ │
│  │  • CreatedAt: time.Time                 │ │
│  │                                          │ │
│  └──────────────┬───────────────────────────┘ │
│                 │                              │
│                 ↓                              │
│  ┌──────────────────────────────────────────┐ │
│  │            Peer Model                    │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  • ID: string                           │ │
│  │  • Conn: *websocket.Conn                │ │
│  │  • Role: string (sender/receiver)       │ │
│  │  • SendChan: chan SignalMessage         │ │
│  │                                          │ │
│  └──────────────────────────────────────────┘ │
│                                                │
└────────────────────────────────────────────────┘
```

---

## Frontend Architecture

```
┌────────────────────────────────────────────────┐
│          Browser (app.js)                      │
├────────────────────────────────────────────────┤
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │    SecureFileVault Class                 │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  State:                                  │ │
│  │  • role: 'sender' | 'receiver'          │ │
│  │  • sessionId: string                    │ │
│  │  • ws: WebSocket                        │ │
│  │  • pc: RTCPeerConnection                │ │
│  │  • dataChannel: RTCDataChannel          │ │
│  │  • encryptionKey: CryptoKey             │ │
│  │  • iv: Uint8Array                       │ │
│  │  • file: File                           │ │
│  │  • receivedChunks: ArrayBuffer[]        │ │
│  │                                          │ │
│  └──────────────┬───────────────────────────┘ │
│                 │                              │
│                 ↓                              │
│  ┌──────────────────────────────────────────┐ │
│  │    Signaling Module                      │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  • connectWebSocket()                   │ │
│  │  • sendSignal(msg)                      │ │
│  │  • handleSignalMessage(msg)             │ │
│  │                                          │ │
│  └──────────────┬───────────────────────────┘ │
│                 │                              │
│                 ↓                              │
│  ┌──────────────────────────────────────────┐ │
│  │    WebRTC Module                         │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  • createPeerConnection()               │ │
│  │  • setupDataChannel()                   │ │
│  │  • createOffer()                        │ │
│  │  • handleOffer(offer)                   │ │
│  │  • handleAnswer(answer)                 │ │
│  │  • handleICE(candidate)                 │ │
│  │                                          │ │
│  └──────────────┬───────────────────────────┘ │
│                 │                              │
│                 ↓                              │
│  ┌──────────────────────────────────────────┐ │
│  │    Encryption Module                     │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  • generateEncryptionKey()              │ │
│  │  • exportKey(key)                       │ │
│  │  • importKey(keyData)                   │ │
│  │  • encryptChunk(chunk, index)           │ │
│  │  • decryptChunk(chunk, index)           │ │
│  │  • calculateHash(data)                  │ │
│  │                                          │ │
│  └──────────────┬───────────────────────────┘ │
│                 │                              │
│                 ↓                              │
│  ┌──────────────────────────────────────────┐ │
│  │    Transfer Module                       │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  • sendFile()                           │ │
│  │  • handleDataChannelMessage(data)       │ │
│  │  • reassembleFile()                     │ │
│  │  • updateProgress()                     │ │
│  │                                          │ │
│  └──────────────┬───────────────────────────┘ │
│                 │                              │
│                 ↓                              │
│  ┌──────────────────────────────────────────┐ │
│  │    UI Module                             │ │
│  ├──────────────────────────────────────────┤ │
│  │                                          │ │
│  │  • showScreen(id)                       │ │
│  │  • updateStatus(message)                │ │
│  │  • showError(message)                   │ │
│  │  • updateProgress()                     │ │
│  │                                          │ │
│  └──────────────────────────────────────────┘ │
│                                                │
└────────────────────────────────────────────────┘
```

---

## Network Protocol Stack

```
┌────────────────────────────────────────┐
│     Application Protocol               │
│                                        │
│  Metadata Message (JSON):              │
│  {                                     │
│    type: "metadata",                   │
│    data: { key, iv, hash, ... }       │
│  }                                     │
│                                        │
│  Encrypted Chunks (Binary):            │
│  ArrayBuffer of encrypted data         │
│                                        │
│  Complete Message (JSON):              │
│  { type: "complete" }                  │
└───────────────┬────────────────────────┘
                │
                ↓
┌────────────────────────────────────────┐
│     WebRTC DataChannel                 │
│     (Reliable, Ordered)                │
└───────────────┬────────────────────────┘
                │
                ↓
┌────────────────────────────────────────┐
│     DTLS (Encryption + Auth)           │
└───────────────┬────────────────────────┘
                │
                ↓
┌────────────────────────────────────────┐
│     SCTP (Stream Control)              │
└───────────────┬────────────────────────┘
                │
                ↓
┌────────────────────────────────────────┐
│     UDP (or TCP fallback)              │
└───────────────┬────────────────────────┘
                │
                ↓
┌────────────────────────────────────────┐
│     IP (Internet Layer)                │
└────────────────────────────────────────┘
```

---

## Message Sequence Diagram (Complete Flow)

```
Sender          SignalServer        Receiver
  │                  │                  │
  │──WebSocket───────→│                  │
  │  Connect          │                  │
  │                   │                  │
  │──Join─────────────→│                  │
  │  (sender)         │                  │
  │                   │                  │
  │                   ├─Create Session   │
  │                   │  Generate ID     │
  │                   │                  │
  │←─Ready────────────┤                  │
  │  (sessionId)      │                  │
  │                   │                  │
  │                   │←──WebSocket──────┤
  │                   │   Connect        │
  │                   │                  │
  │                   │←──Join───────────┤
  │                   │   (receiver, ID) │
  │                   │                  │
  │                   ├─Match Session    │
  │                   │                  │
  │                   ├──Ready──────────→│
  │                   │                  │
  │─Create PC─┐       │                  │
  │           │       │                  │
  │←──────────┘       │                  │
  │                   │                  │
  │─Create DC─┐       │                  │
  │           │       │                  │
  │←──────────┘       │                  │
  │                   │                  │
  │─SDP Offer─┐       │                  │
  │           │       │                  │
  │←──────────┘       │                  │
  │                   │                  │
  │──Offer────────────→│                  │
  │                   │                  │
  │                   ├──Offer──────────→│
  │                   │                  │
  │                   │                  │─Create PC─┐
  │                   │                  │           │
  │                   │                  │←──────────┘
  │                   │                  │
  │                   │                  │─SDP Answer┐
  │                   │                  │           │
  │                   │                  │←──────────┘
  │                   │                  │
  │                   │←─Answer──────────┤
  │                   │                  │
  │←─Answer───────────┤                  │
  │                   │                  │
  │──ICE──────────────→│──ICE────────────→│
  │←─ICE──────────────┤←─ICE─────────────┤
  │                   │                  │
  ╞═══════════════════════════════════════╡
  │        DataChannel Connected         │
  ╞═══════════════════════════════════════╡
  │                   │                  │
  │─Generate Key      │                  │
  │─Generate IV       │                  │
  │─Calculate Hash    │                  │
  │                   │                  │
  │──Metadata────────────────────────────→│
  │  (key, iv, hash)  │                  │─Import Key
  │                   │                  │
  │──Chunk 0─────────────────────────────→│─Store
  │──Chunk 1─────────────────────────────→│─Store
  │──Chunk 2─────────────────────────────→│─Store
  │      ...          │                  │  ...
  │──Chunk N─────────────────────────────→│─Store
  │                   │                  │
  │──Complete────────────────────────────→│
  │                   │                  │
  │                   │                  │─Decrypt All
  │                   │                  │─Reassemble
  │                   │                  │─Verify Hash
  │                   │                  │
  │                   │                  │─✅ Success
  │                   │                  │─Ready to Download
  │                   │                  │
```

---

## Deployment Architecture

```
                    Internet
                       │
                       ↓
             ┌─────────────────┐
             │   Load Balancer │
             │     (nginx)     │
             └────────┬────────┘
                      │
         ┌────────────┴────────────┐
         ↓                         ↓
┌─────────────────┐       ┌─────────────────┐
│   Go Server 1   │       │   Go Server 2   │
│   (Backend)     │       │   (Backend)     │
│                 │       │                 │
│  Port: 8080     │       │  Port: 8080     │
└─────────────────┘       └─────────────────┘
         │                         │
         └────────────┬────────────┘
                      │
              ┌───────────────┐
              │  STUN Server  │
              │  (Optional)   │
              └───────────────┘

Note: WebRTC P2P connections bypass servers
```

---

This architecture provides:
- ✅ **Scalability**: Stateless signaling servers
- ✅ **Security**: Multi-layer encryption
- ✅ **Privacy**: Zero-knowledge server
- ✅ **Performance**: Direct P2P transfer
- ✅ **Simplicity**: Minimal infrastructure

---

**Document Version:** 1.0  
**Last Updated:** January 2026
